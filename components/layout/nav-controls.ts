/** Shared navbar control sizing so every action aligns on one row. */
export const navControlBase =
  "inline-flex h-10 shrink-0 items-center justify-center rounded-xl border border-fuchsia-300/30 bg-theme-interactive/90 dark:border-fuchsia-300/20 dark:bg-theme-elevated";

export const navIconControl = `${navControlBase} w-10`;

export const navTextControl = `${navControlBase} px-3 text-xs font-semibold`;
